version: '3.8'

services:
  uptime-kuma:
    container_name: uptime-kuma-dev
    image: louislam/uptime-kuma:nightly2
    ports:
      - "3001:3001"  # <Host Port>:<Container Port>
      - "3307:3306"

